CREATE TABLE [ops].[_columnTypes] (
    [id]         SMALLINT      IDENTITY (1, 1) NOT NULL,
    [columnType] VARCHAR (100) NOT NULL,
    CONSTRAINT [PK__columnTypes] PRIMARY KEY CLUSTERED ([id] ASC)
);


GO

